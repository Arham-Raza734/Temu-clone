# Express server configuration
PORT=5000

# MongoDB Atlas connection string (or local connection string)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.zfflf2d.mongodb.net/temu_clone?retryWrites=true&w=majority
